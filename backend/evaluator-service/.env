# Postgres connection string
DB_URL=postgresql://postgres:root@localhost:5432/healthguard
# if running in Docker, host might be "db" instead of "localhost"

# Gemini
GEMINI_API_KEY=AIzaSyBNYWK5x4YhIw4LsuQh2akI9duBNWdR8RE
GEMINI_JUDGE_MODEL=gemini-2.0-flash

# Latency normalization (same as eval harness)
LATENCY_MAX_MS=3000
